// Re-export the generated Convex API and types for use in the mobile app.
// All screens should import { api } and { Id } from this file.
export { api } from "@reread/convex"
export type { Id } from "@reread/convex/dataModel"
